export type Article = {
  id: string
  imageUrl: string
  artist: string
  band: string
  createdAt: Date
}

export type Effector = {
  id: string
  name: string
  brand: string
  type: string
  createdAt: Date
}
